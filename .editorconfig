# EditorConfig ajuda a manter estilos consistentes entre VS Code e Visual Studio
# Mais informações em https://editorconfig.org

root = true

# 1. Configurações Globais (Padrão para C# e a maioria dos arquivos)
[*]
charset = utf-8
indent_style = space
indent_size = 4
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true

# 2. Configurações para C# e Blazor (Razor)
# Blazor (.razor) mistura C# e HTML. O padrão do VS é 4 espaços.
[*.{cs,razor}]
indent_size = 4
csharp_style_namespace_declarations = file_scoped:suggestion
csharp_style_var_for_built_in_types = true:suggestion
csharp_style_var_when_type_is_apparent = true:suggestion

# 3. Arquivos Web "Puros" e Configurações (JSON, YAML, HTML estático)
# Frontend moderno geralmente usa 2 espaços
[*.{html,js,css,scss,json,yml,yaml}]
indent_size = 2

# 4. Documentação (Markdown)
# Mantém indentação de 2 espaços e PERMITE espaços em branco no final (para quebra de linha)
[*.{md,markdown}]
indent_size = 2
trim_trailing_whitespace = false
